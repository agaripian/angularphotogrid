/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GridContainerSortableComponent } from './grid-container-sortable.component';

describe('Component: GridContainerSortable', () => {
  it('should create an instance', () => {
    let component = new GridContainerSortableComponent();
    expect(component).toBeTruthy();
  });
});
